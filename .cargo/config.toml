# Intentionally empty: rustc-lld rejects gcc -rdynamic, and rustc-link-arg
# --export-dynamic broke `cargo test --release` on ubuntu-latest. IO plugins
# still resolve via -ldl; host-symbol export can be restored later if needed.
